{% extends 'base.html.twig' %}

{% block title %}Ajouter un Chat{% endblock %}

{% block body %}
    <h1>Ajouter un Chat</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-primary">Enregistrer</button>
    {{ form_end(form) }}
{% endblock %}
